import { Button } from "antd";
import { memo } from "react";
import { Link } from "react-router-dom";

const Home = memo(() => {
    return (

        <div >
            home
        </div>
    );
});

export default Home;